# Project dependencies
#
# This project requires Python 3 and Bash; it has no Python package
# dependencies. Add project-specific runtime dependencies below using exact
# version pins:
#
# package-name==1.2.3  # purpose, ownership boundary, and license
#
# Document optional development tools separately. Add a new version pin only
# after reviewing its license, security, and compatibility.
