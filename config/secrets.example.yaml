# Nur Namen und Platzhalter. Diese Datei niemals mit echten Werten ausfüllen.
# Eine lokale Kopie als config/secrets.yaml ist durch .gitignore ausgeschlossen.

integration:
  username: "<SERVICE_USERNAME>"
  credential: "<SERVICE_CREDENTIAL>"
